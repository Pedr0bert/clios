// Função que será chamada como um comando
fn hello(args) {
    print("Olá vindo do Plugin Compilado (AST)!");
    print("Argumentos recebidos: " + args);
}

fn soma(args) {
    // Args vem como lista de strings, converter se precisar
    print("Calculando algo complexo...");
}
